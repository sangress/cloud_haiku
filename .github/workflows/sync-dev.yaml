name: Sync dev with main
on:
  push:
    branches: [main]

  workflow_dispatch:

jobs:
  sync_dev:
    runs-on: ubuntu-latest
    steps:
      -name: Checkout
       uses: actions/checkout@v1
      -name: Run script
       run: ./bin/sync-dev.sh .
